/* ── Global ─────────────────────────────────────────── */

Screen {
    background: $background;
}

/* ── Main layout ────────────────────────────────────── */

#main-layout {
    height: 1fr;
    layout: horizontal;
}

#file-status {
    width: 36;
    border: solid $primary-darken-2;
}

#right-panel {
    width: 1fr;
    layout: vertical;
}

CommitLog {
    height: 1fr;
    border: solid $primary-darken-2;
}

DiffView {
    height: 1fr;
    border: solid $primary-darken-2;
}

/* ── Modal dialogs ──────────────────────────────────── */

RepoPickerScreen,
SSHScreen,
CommitScreen {
    align: center middle;
    background: $background 70%;
}

#picker-form,
#ssh-form,
#commit-dialog {
    background: $surface;
    border: thick $primary;
    padding: 1 2;
    width: 64;
    height: auto;
}

.form-title {
    text-align: center;
    text-style: bold;
    color: $accent;
    padding-bottom: 1;
}

.form-label {
    color: $text-muted;
    margin-top: 1;
}

.error-msg {
    color: $error;
    height: 1;
}

Input {
    margin-bottom: 0;
}

Button {
    margin-top: 1;
    margin-right: 1;
}
